# Base runtime dependencies for the project.
# On Windows, use `setup_env.ps1` to install the compatible `fast-safe-rl` stack.
numpy<2
torch
gymnasium
highway-env
tianshou~=0.5.1
matplotlib
scipy
pandas
seaborn
imageio[ffmpeg]
pyrallis==0.3.1
pyyaml~=6.0
wandb~=0.14.0
protobuf~=3.19.0
prettytable~=3.7.0
typing-inspect
